{
  "name": "jsx-ssr",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild --bundle --outdir=dist ./src/index.tsx",
    "dev": "miniflare --live-reload",
    "deploy": "wrangler publish src/index.tsx"
  },
  "license": "MIT",
  "dependencies": {
    "hono": "3.0.0-0"
  },
  "devDependencies": {
    "esbuild": "^0.15.1",
    "miniflare": "^2.9.0",
    "wrangler": "^2.10.0"
  }
}
